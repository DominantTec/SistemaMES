version: "3.9"

services:
  mssql:
    build: .
    container_name: mssql-server
    ports:
      - "8080:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=${MSSQL_SA_PASSWORD}
    volumes:
      - mssql-data:/var/opt/mssql
    restart: always

volumes:
  mssql-data: